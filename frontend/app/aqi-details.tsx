import { ScrollView, Text } from 'react-native';
import Header from '@/components/Header';

export default function AQIDetails() {
  return (
    <ScrollView>
      <Header />
      <Text> AQI Details </Text>
    </ScrollView>
  );
}
